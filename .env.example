# Copy this file to .env for local development. The atmos weather service
# needs NO secrets - Open-Meteo is keyless. Everything below is optional and
# has a sensible code default.

# Port for the MCP streamable-HTTP endpoint (default 8000).
MCP_PORT=8000

# Default city used by the demo/example scripts.
ATMOS_DEMO_CITY=Madrid
